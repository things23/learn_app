default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: localhost
  username: flashcardr
  password: MoAnTgBB

development:
  <<: *default
  database: flashcardr_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  #<<: *default
  #database: flashcardr_test
  adapter: postgresql
  database: travis_ci_test
  username: postgres

production:
  <<: *default